// Placeholder module intentionally left blank.
// Previously used for server-side PDF rasterization.
// To avoid bundling heavy native deps in Next.js build, all rasterization moved to CLI script: scripts/rasterize-pdfs.mjs
export {}; // keep as a module
